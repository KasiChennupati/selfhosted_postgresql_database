# Postgres Container Image Versions

This project supports multiple versions of self hosted postgres containers and each container image version required build Containerfile/Dockerfile are present in here.
